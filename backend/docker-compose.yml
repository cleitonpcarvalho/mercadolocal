services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    ports:
      - "8001:8001"
    volumes:
      - ./:/app
    command: gunicorn core.wsgi:application --bind 0.0.0.0:8001 --reload
